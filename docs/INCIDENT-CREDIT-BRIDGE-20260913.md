# 尾号 5728 账户积分显示为零排查

## 已核实事实

核查日期：2026-09-13。使用生产容器现有凭据，只读查询指定账户的 `user_profiles`、`auth_user_bridges`、`orders`、`user_credits` 和完整积分流水。未修改数据库、订单、余额或会员状态。

- 当前生产提交为 `7ba9c3b5`。
- 尾号 5728 的手机号精确匹配一个旧 Authing 身份及一个 Supabase 统一身份；两者之间有服务端保存的明确桥接关系。
- 订单 168：2026-05-10 支付 68 元购买专业版，`credits_amount=5000`，状态 `paid`。
- 流水 2746：充值前 84、充值 5000、充值后 5084。
- 随后六笔使用合计扣除 450，余额 4634。
- 2026-08-29 的成对流水 8160/8161 将 4634 从旧账户转入统一账户。旧账户归零且 `is_pro=false`；统一账户保留积分和专业版标记。
- 当日两次作文批改（流水 8162/8163）分别扣除 100 和 111，目前统一账户余额 **4423**，`is_pro=true`。

5 月这笔 5000 积分购买之后的账本连续，余额可核算为 `84 + 5000 - 450 - 211 = 4423`。没有证据表明这笔购买积分因三个月到期而被清除。更早的历史流水存在余额衔接缺口，本次不据此追加补偿或推断原因。

## 根因与修复

旧生产逻辑优先采用最近付费会员订单所属身份的积分行。订单仍在旧身份下，积分已迁移到统一身份，导致查询选中旧身份的零余额。作文批改使用同一权益解析函数选择扣费账户，因此会影响余额显示和可用积分判断。

积分明细接口仅查询登录身份，也会将迁移转出误计为消费、迁移转入误计为收入。

修复范围：

1. `lib/user-entitlements.ts`：通过明确的服务端桥接关系关联旧、新身份，迁移完成后优先读取统一账户；保留尚未迁移的旧余额；不因余额实际为零重新赠分。独立查询并发执行，以减少积分接口等待。
2. `app/api/user/transactions/route.ts`：明细读取明确桥接的两个身份，并在用户视图中排除这一桥接的成对内部转账。数据库审计流水保持原样，其他用户记录不可查询。
3. 保留并验证既有 Next.js 16 构建修复：ChatInput 的 `use client` 放在导入前，两个动态路由的 `params` 使用 Promise 类型。

不需要给该账户再次补发 5000 积分。修复通过读取原有 4423 积分恢复使用。

## 验证

- 在生产提交对应的隔离代码上用去标识化账户数据重放，最初 6 项用例中有 4 项失败，复现旧身份返回零余额和历史割裂；随后补齐为 8 项针对性回归。
- 修复后 5 套 Jest、62 项用例通过，覆盖双身份余额与历史、实际零余额、迁移未完成、无桥接、查询失败、越权拒绝和支付安全回归。
- 目标文件 ESLint 通过。
- 完整 Webpack 生产构建（含 TypeScript 和 85 个页面生成）通过。隔离构建使用无权限的测试配置，构建产物只用于本地验证，不用于生产发布。
- 本机 Chrome 手机尺寸 390×844 验证通过：从 `/credits` 入口打开页面，经真实 Next.js 路由查询本地模拟身份/账本服务，显示余额 4423、旧购买和新消费记录；内部转账不出现在消费统计中；刷新后仍为 4423。模拟服务仅接受 GET，不进行生产访问或数据库写入。
- 浏览器截图：`/private/tmp/credit-bridge-mobile.png`；复现脚本：`/private/tmp/verify-credit-bridge.cjs`。这属于本地端到端验证，并非生产用户登录验证。

## 发布边界

本次积分补丁基于当前生产提交独立准备，不含上一个任务尚待授权的邮箱修复，也不含主工作区的其他网关、人工补分脚本或数据库迁移。

之前自动审批拒绝生产源码传输，要求明确授权。此文档记录准备结果，不代表已经上线。发布需要将已审查提交同步至 `43.154.111.156:/data/ai-essay-editor`，使用现有环境和应用构建配置重建 `shenxiang-nextjs`；不修改网关、1Panel 或持久化数据。回滚只需还原本次应用提交并重建旧版本。

发布后核查健康检查版本、容器编译产物，并用测试账号从登录入口进入积分页，核对余额和流水；尾号 5728 用户的真实会话最终反馈仍需其刷新后确认。
