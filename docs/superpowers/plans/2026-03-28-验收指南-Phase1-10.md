# 服务器重建项目验收指南

**版本**: v1.7
**验收范围**: 阶段一 ~ 阶段十
**验收人**: Gemini (后端)
**日期**: 2026-03-28

---

## 阶段一：服务器初始化 (Day 1)

### 1.1 服务器系统准备

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Ubuntu 24.04 LTS 已安装 | `cat /etc/os-release` | VERSION="24.04 LTS" |
| 系统已更新 | `apt list --upgradable` | 无可更新包 |
| 基础工具已安装 | `which curl wget git vim htop net-tools` | 全部返回路径 |
| SSH 密钥登录可用 | `cat ~/.ssh/authorized_keys` | 包含公钥内容 |
| 防火墙已配置 | `ufw status` | 22, 80, 443 端口已放通 |
| DNS 已配置 | `ping -c 1 api.shenxiang.school` | 能解析到服务器IP |

### 1.2 磁盘分区 (如有数据盘)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 数据盘已挂载 | `df -h /data` | 显示 /dev/vdb 或类似 |
| fstab 已配置 | `grep /data /etc/fstab` | 包含 /data 挂载条目 |

---

## 阶段二：1Panel 面板安装 (Day 1)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 1Panel 已安装 | `curl -s http://localhost:20241/api/panelinfo` 或浏览器访问 | 返回面板信息 |
| 端口已更改 | `ss -tlnp \| grep 20241` | 监听中 (如使用非默认端口) |
| 防火墙放通 | `ufw status \| grep 20241` 或控制台验证 | 已放通或控制台可访问 |

---

## 阶段三：Docker 环境 (Day 1)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Docker 已安装 | `docker --version` | 显示 Docker 版本 |
| Docker Compose 已安装 | `docker-compose --version` | 显示 docker-compose 版本 |
| 镜像加速已配置 | `cat /etc/docker/daemon.json` | 包含 registry-mirrors |
| Docker 已启动 | `systemctl status docker` | active (running) |
| Docker 开机自启 | `systemctl is-enabled docker` | enabled |
| shenxiang-net 网络存在 | `docker network ls \| grep shenxiang-net` | 显示网络 |

---

## 阶段四：SSL 证书申请 (Day 1)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Certbot 已安装 | `which certbot` | 返回路径 |
| SSL 证书已申请 | `ls -la /etc/letsencrypt/live/api.shenxiang.school/` | 包含 fullchain.pem, privkey.pem |
| 证书有效 | `openssl s_client -connect api.shenxiang.school:443 -servername api.shenxiang.school </dev/null 2>/dev/null \| openssl x509 -noout -dates` | NotBefore, NotAfter 合理 |
| 自动续期已配置 | `certbot renew --dry-run` | 模拟成功，无错误 |

---

## 阶段五：Redis 部署 (Day 2)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Redis 容器运行中 | `docker ps \| grep shenxiang-redis` | 显示容器 |
| Redis 配置正确 | `docker exec shenxiang-redis cat /usr/local/etc/redis/redis.conf \| grep requirepass` | 显示密码配置 |
| Redis 密码验证 | `docker exec shenxiang-redis redis-cli -a [密码] ping` | 返回 PONG |
| 容器加入 shenxiang-net | `docker network inspect shenxiang-net \| grep shenxiang-redis` | 能找到 |
| 数据持久化 | `ls -la /data/redis/data/` | 存在数据目录 |

---

## 阶段六：Dify 部署 (Day 2-3)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Dify 容器运行中 | `docker ps \| grep shenxiang-dify` | 显示容器 |
| Dify 健康检查 | `curl http://localhost:8080/health` | 返回 `{"status":"ok"}` |
| Dify 状态检查 | `curl http://localhost:8080/operations/status` | 返回状态信息 |
| Supabase PostgreSQL 连接 | 检查 Dify 日志无连接错误 | `docker logs shenxiang-dify 2>&1 \| grep -i error` 无 Supabase 相关错误 |
| Redis 连接 | 检查 Dify 日志无 Redis 错误 | 无 Redis 连接错误 |
| 容器加入 shenxiang-net | `docker network inspect shenxiang-net \| grep shenxiang-dify` | 能找到 |

---

## 阶段七：MinIO 对象存储部署 (Day 3)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| MinIO 容器运行中 | `docker ps \| grep shenxiang-minio` | 显示容器 |
| MinIO 健康检查 | `curl http://localhost:9000/minio/health/live` | 返回 `{"status":"ok"}` |
| MinIO Console 可访问 | 浏览器访问 `http://服务器IP:9001` | 显示登录页面 |
| 存储桶已创建 | `docker exec shenxiang-minio mc ls myminio/` | 显示 shenxiang-uploads 桶 |
| 存储桶公开策略 | `docker exec shenxiang-minio mc anonymous get shenxiang-uploads` | 显示 download, upload 策略 |
| 容器加入 shenxiang-net | `docker network inspect shenxiang-net \| grep shenxiang-minio` | 能找到 |

---

## 阶段八：Next.js 部署 (Day 3-4)

### 8.1-8.6 Next.js 应用

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Next.js 容器运行中 | `docker ps \| grep shenxiang-nextjs` | 显示容器 |
| Next.js 健康检查 | `curl http://localhost:3000/health` | 返回 `{"status":"ok",...}` |
| 容器加入 shenxiang-net | `docker network inspect shenxiang-net \| grep shenxiang-nextjs` | 能找到 |
| .env 文件存在 | `cat /data/nextjs/.env` | 包含所有必要环境变量 (非空) |
| 环境变量加载正确 | 检查 Supabase URL、Dify URL 等 | 值正确配置 |

### 8.7 Webhook 自动部署

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Webhook systemd 服务存在 | `systemctl status webhook` | 显示服务状态 |
| Webhook 服务运行中 | `systemctl is-active webhook` | active |
| Webhook 服务开机自启 | `systemctl is-enabled webhook` | enabled |
| GITHUB_WEBHOOK_SECRET 已设置 | `grep GITHUB_WEBHOOK_SECRET /etc/systemd/system/webhook.service` | 显示非默认值 |
| Webhook 端口监听 | `ss -tlnp \| grep 3001` | 监听 3001 端口 |
| deploy.sh 存在且可执行 | `ls -la /data/nextjs/webhook/deploy.sh` | -rwx 权限 |
| Webhook 能接收请求 | `curl -X POST http://localhost:3001/webhook -d '{}'` | 返回 Forbidden 或 OK (非 404) |

---

## 阶段九：Nginx 配置 (Day 4)

| 验收项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| Nginx systemd 服务存在 | `systemctl status nginx` | 显示服务状态 |
| Nginx 运行中 | `systemctl is-active nginx` | active |
| Nginx 开机自启 | `systemctl is-enabled nginx` | enabled |
| SSL 站点配置存在 | `ls -la /etc/nginx/sites-enabled/api.shenxiang.school` | 链接到 sites-available |
| Nginx 配置语法正确 | `nginx -t` | syntax is ok |
| HTTPS 可访问 | `curl -I https://api.shenxiang.school` | 200 OK 或 302 重定向 |
| SSE 配置正确 | 检查 nginx.conf 或站点配置 | 包含 `proxy_buffering off; proxy_read_timeout 600s;` |
| CORS 配置正确 | `curl -I https://api.shenxiang.school -H "Origin: https://shenxiang.school"` | 包含 CORS 头 |
| 代理到 Next.js | `curl -I http://shenxiang-nextjs:3000/health` (服务器上) | 返回健康状态 |
| 文件上传大小限制 | 检查配置 `client_max_body_size` | 100M 或更大 |

---

## 阶段十：整体验证 (Day 4-5)

### 10.1 服务健康检查

| 服务 | 验证命令 | 预期结果 |
|------|----------|----------|
| Nginx | `curl -I https://api.shenxiang.school` | 200 OK |
| Next.js | `curl http://localhost:3000/health` | {"status":"ok"} |
| Redis | `docker exec shenxiang-redis redis-cli -a [密码] ping` | PONG |
| Dify | `curl http://localhost:8080/health` | {"status":"ok"} |
| MinIO | `curl http://localhost:9000/minio/health/live` | {"status":"ok"} |

### 10.2 Docker 网络验证

```bash
# 检查 shenxiang-net 网络
docker network inspect shenxiang-net

# 验证容器间通信
docker exec shenxiang-nextjs ping -c 1 shenxiang-dify
docker exec shenxiang-nextjs ping -c 1 shenxiang-redis
docker exec shenxiang-dify ping -c 1 shenxiang-redis
```

### 10.3 功能测试 (可选，如前端已对接)

| 功能 | 测试方法 | 验证点 |
|------|----------|--------|
| 用户登录 | 前端登录页面 | 登录成功，能获取用户信息 |
| 积分查询 | 调用 /api/user/credits | 返回正确余额 |
| 聊天对话 | 发送简单问题 | SSE 流式响应 |
| 文件上传 | 上传测试文件 | 上传成功，返回文件URL |

---

## 验收签字

| 角色 | 姓名 | 日期 | 签字 |
|------|------|------|------|
| 后端验收 | Gemini | 2026-03-28 | _____________ |
| 前端验收 | Claude Code | ________ | _____________ |

---

## 附录：常用命令

```bash
# 查看所有容器状态
docker ps -a

# 查看所有服务状态
systemctl list-units --type=service --state=running

# 查看容器日志
docker logs -f [容器名]

# 重启服务
systemctl restart [服务名]

# 检查端口占用
ss -tlnp | grep [端口]
```

---

**文档版本**: v1.0
**创建日期**: 2026-03-28
**状态**: 待验收
