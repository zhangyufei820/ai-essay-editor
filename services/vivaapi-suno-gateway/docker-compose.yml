services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${HOST_PORT:-8000}:8000"
    networks:
      default:
      shenxiang-net:
        aliases:
          - vivaapi-suno-gateway

networks:
  shenxiang-net:
    external: true
