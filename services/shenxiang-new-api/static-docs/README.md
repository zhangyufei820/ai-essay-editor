# New API static docs

This directory tracks the public static documentation files served for `https://api.aiphui.top/docs/`.

Production path:

```text
/etc/nginx/conf.d/shenxiang-new-api-static/docs/
```

The Nginx route serves these files directly with `root /etc/nginx/conf.d/shenxiang-new-api-static;`.
